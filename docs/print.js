window.MIRPrint={open(session){const w=window.open('','_blank');if(!w)throw new Error('Permite ventanas emergentes.');const qs=session.items.filter(x=>x.kind==='question');w.document.write(`<!doctype html><meta charset="utf-8"><title>${session.title}</title><style>@page{size:A4;margin:18mm}body{font-family:Arial;line-height:1.5}section{margin:0 0 22px;break-inside:avoid}h1{font-size:24px}.opt{margin:5px 0}.no-print{position:sticky;top:0;background:#eee;padding:10px}@media print{.no-print{display:none}}</style><div class="no-print"><button onclick="print()">Imprimir / Guardar PDF</button></div><h1>${session.title}</h1><p>${session.date} · ${qs.length} preguntas</p>${qs.map((x,i)=>`<section><b>${i+1}. ${x.data.stem}</b>${x.data.options.map((o,j)=>`<div class="opt">${j+1}. ${o}</div>`).join('')}</section>`).join('')}`);w.document.close();}};