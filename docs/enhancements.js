(()=>{'use strict';
const qs=s=>document.querySelector(s);
const html=document.documentElement;
const prefKey='mir27.ui.v1';
const prefs=(()=>{try{return JSON.parse(localStorage.getItem(prefKey)||'{}')}catch{return{}}})();
function save(){localStorage.setItem(prefKey,JSON.stringify(prefs));}
function apply(){html.classList.toggle('focus-mode',!!prefs.focus);html.classList.toggle('compact-mode',!!prefs.compact);html.style.setProperty('--reader-scale',String(prefs.scale||1));}
function toast(msg){let t=qs('#toast');if(!t){t=document.createElement('div');t.id='toast';document.body.appendChild(t);}t.textContent=msg;t.style.display='block';setTimeout(()=>t.style.display='none',1800);}
function controls(){if(qs('#study-tools'))return;const box=document.createElement('div');box.id='study-tools';box.className='study-tools';box.innerHTML=`<button title="Modo foco (F)" data-ui="focus">Foco</button><button title="Vista compacta" data-ui="compact">Compacto</button><button title="Reducir texto" data-ui="smaller">A−</button><button title="Aumentar texto" data-ui="larger">A+</button><button title="Atajos de teclado" data-ui="help">?</button>`;document.body.appendChild(box);box.onclick=e=>{const b=e.target.closest('[data-ui]');if(!b)return;const a=b.dataset.ui;if(a==='focus')prefs.focus=!prefs.focus;if(a==='compact')prefs.compact=!prefs.compact;if(a==='smaller')prefs.scale=Math.max(.85,(prefs.scale||1)-.05);if(a==='larger')prefs.scale=Math.min(1.3,(prefs.scale||1)+.05);if(a==='help')alert('Atajos MIR/27\n\n1–4: elegir opción\nEnter: confirmar / siguiente\nF: modo foco\nEsc: salir de foco\n?: ayuda');save();apply();};}
function decorate(){controls();const main=qs('#main');if(!main)return;main.querySelectorAll('.question-card').forEach(card=>{if(card.dataset.enhanced)return;card.dataset.enhanced='1';const h=card.querySelector('h2');if(h){const wrap=document.createElement('div');wrap.className='question-heading';h.parentNode.insertBefore(wrap,h);wrap.appendChild(h);}const ex=card.querySelector('.explain');if(ex&&!ex.querySelector('.explain-label')){const tag=document.createElement('div');tag.className='explain-label';tag.textContent='Corrección razonada';ex.prepend(tag);}});main.querySelectorAll('.sourcebox').forEach(x=>x.classList.add('traceability'));}
function currentOptions(){return [...document.querySelectorAll('.question-card .option:not(:disabled)')];}
function clickNext(){const ans=qs('#answer:not(:disabled)');if(ans){ans.click();return true;}const next=qs('#next:not(:disabled)');if(next){next.click();return true;}return false;}
document.addEventListener('keydown',e=>{if(/INPUT|TEXTAREA|SELECT/.test(e.target.tagName))return;if(e.key>='1'&&e.key<='4'){const o=currentOptions()[Number(e.key)-1];if(o){o.click();e.preventDefault();}}else if(e.key==='Enter'){if(clickNext())e.preventDefault();}else if(e.key.toLowerCase()==='f'){prefs.focus=!prefs.focus;save();apply();toast(prefs.focus?'Modo foco activado':'Modo foco desactivado');}else if(e.key==='Escape'&&prefs.focus){prefs.focus=false;save();apply();toast('Modo foco desactivado');}else if(e.key==='?'){alert('Atajos MIR/27\n\n1–4: elegir opción\nEnter: confirmar / siguiente\nF: modo foco\nEsc: salir de foco');}});
const obs=new MutationObserver(decorate);obs.observe(document.body,{childList:true,subtree:true});apply();decorate();
})();